#include "utils.h"
#include <iostream>
#include <fstream>

//bool isWhitespace(char);

//std::string trim(const std::string&);

void write_user_tokens(const char* dir, const utils::checker& checker);

void write_user_nodes(const char* dir, const utils::checker& checker);


